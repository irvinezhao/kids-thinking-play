import type { CapacitorConfig } from '@capacitor/cli'

const config: CapacitorConfig = {
  appId: 'com.irvinezhao.kidsthinkingplay',
  appName: '小小思维岛',
  webDir: 'dist',
  server: {
    androidScheme: 'https',
  },
}

export default config
